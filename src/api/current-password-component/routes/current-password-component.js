'use strict';

/**
 * current-password-component router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::current-password-component.current-password-component');
